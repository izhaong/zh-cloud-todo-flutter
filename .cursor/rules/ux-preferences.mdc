---
description: 跨端 UX 默认偏好（Flutter / UniApp / Vben）：云服务配置、登录态呈现、表单与反馈
globs: "**/*.{dart,vue,ts,tsx,js,jsx}"
alwaysApply: false
---

# UX 默认偏好（本项目）

本规则用于减少“你得把每一步都说很细”的沟通成本：当用户描述目标行为时，Agent **默认按本规则推断正常交互**，除非用户明确要求相反做法。

## 一般原则

- **状态驱动 UI**：优先用“状态→界面”映射（未配置 / 未登录 / 已登录 / 加载中 / 错误 / 空数据），避免同一页面长期保留互斥的多套表单。
- **单一入口与就地完成**：能在当前上下文完成的配置/登录/测试，不要另开页面或藏入口；避免出现“弹窗里又引导去另一个登录页”。
- **可逆与可恢复**：所有会改变后端/本地状态的操作都应可取消、可重试，并对失败给出下一步建议（而不是只吐异常）。
- **最少惊扰**：默认不打断用户主流程。非致命错误用 toast/snackbar；需要用户决策才弹对话框。
- **默认合理**：有既有配置/缓存时，表单默认回填；字段可提供合理默认值（如租户 id 缺省为 `1`）。

## 登录态展示（会员 / 账号体系通用）

- **未登录（需要登录时）**：
  - 显示登录表单（手机号/密码/短信等）与必要的说明。
  - 登录按钮的文案要表达动作（“登录”“发送验证码”），并在请求期间禁用按钮，显示 loading。
- **已登录**：
  - **不要继续显示登录表单**；仅显示简洁的登录信息（例如“已登录为：userId/手机号（若可得）”）与 **退出登录**。
  - 若仍需可改配置（例如 API 根地址/租户），配置区可以保留，但要与登录区做明显分隔（分组标题/间距）。
  - 退出登录后，应立即回到“未登录”态并清空与登录强相关的表单输入（如验证码）。

## 云服务/后端选择与配置（以 `zh-cloud-count` 为代表）

### 选择后端（CloudBackendType / CloudServiceConfig）

- **未配置的后端**：点击卡片应直接进入配置弹窗（或配置页），而不是先切换再报错。
- **已配置的后端**：点击卡片应执行切换（必要时二次确认）；点击“配置/齿轮”才进入配置弹窗。
- **切换的副作用**：
  - 若当前后端需要显式登出（如 Supabase/WebDAV），在切换前处理登出；若后端使用系统账号或独立会话（如 iCloud、芋道会员 Token），按既定策略跳过或单独处理。

### 芋道后端（Yudao backend）交互约定

- **配置弹窗内容应一体化**：在“芋道后端”的配置弹窗内完成
  - API 根地址与租户配置
  - 会员登录/登出（Token 会话）
- **避免重复入口**：当弹窗已包含登录能力时，不在同一页面再放“会员登录与 API 设置”的二级入口按钮（除非它指向更高级设置且有明确区分）。
- **弹窗内登录**：
  - 若用户在弹窗里直接进行登录/发短信，默认使用弹窗输入框中的 API/租户（即使用户还没点“确定”保存配置）。
  - 弹窗底部“确定”负责**校验并持久化**配置（写入 runtime + store），成功后关闭弹窗并给出“已保存”反馈。

## 表单与校验

- **就地校验**：必填项为空、格式不合法时，优先在字段附近展示错误；只有在无法就地表达或需要聚合提示时才用弹窗错误。
- **异步保护**：异步操作结束后更新 UI 前必须检查 mounted（Flutter）/组件存活（Vue）。
- **输入体验**：
  - URL/手机号/验证码等设置合适的 keyboardType/inputmode。
  - 密码默认隐藏；可选提供“显示/隐藏”。

## Loading / 错误 / 空状态

- **Loading**：列表/卡片局部 loading 优先，避免整页白屏；但关键流程（如保存配置、登录）需要明显的“正在进行中”反馈。
- **错误文案**：
  - 面向用户的错误文案不直接展示生硬的堆栈/内部字段；可在 debug 日志保留细节。
  - 提供“下一步”提示（检查网络、检查 API 地址、重试）。
- **空状态**：给出“为什么是空 + 我能做什么”的引导（例如先配置云服务/先登录/先创建账本）。

## 反馈与提示（toast/snackbar/dialog）

- **toast/snackbar**：用于“已保存”“已切换”“登录成功”等轻量反馈。
- **dialog**：用于需要确认的风险操作（切换后端导致登出、清空本地、删除等）或需要用户补充信息。

## 对 Agent 的默认行为约束（减少反复指令）

- 当用户描述“操作应该是 …”时，Agent 需要主动把它转为：
  - **状态机**（未配置/未登录/已登录）
  - **主要入口**（在哪里点、弹什么、完成什么）
  - **反例检查**（是否出现重复入口、已登录仍显示登录表单、配置被分散到多页面）
- 若缺少细节，默认遵循本规则中的“正常交互”，不要反复追问每一步；仅在存在明显分歧（安全/数据丢失/权限）时才提问或给出可选项。
