---
description: CI/Jenkinsfile 与本地 MCP：禁止把密钥与 webhook 默认值写入仓库
globs:
  - "**/Jenkinsfile"
  - "script/jenkins/**/*"
  - ".cursor/mcp*.json"
alwaysApply: false
---

# CI 密钥卫生

## 禁止

- Jenkinsfile 中 **飞书 Webhook URL**、**Gitea credentials UUID**、**MinIO AK/SK**、**数据库口令** 的硬编码默认值（可用空串 + 说明，实际值走 Jenkins Credentials / Folder 环境变量）
- 将含 `Authorization: Basic …`、MinIO AK/SK 的 `.cursor/mcp.json` **提交**到 Git（须 **`.gitignore`**；结构见 **`.cursor/mcp.json.example`** 与 **`.cursor/README-mcp.md`**）

## 推荐

- Folder `zh-cloud-test` / `zh-cloud-prod` 分别注入 `FEISHU_WEBHOOK_URL`、`MINIO_BUCKET`、`DEPLOY_TARGET_DIR`、`UPDATES_PUBLIC_BASE_URL` 等
- 轮换泄露过的 Token

## 本地 MCP 凭据（不入库 + env 引用）

`.claude/mcp.json` / `.cursor/mcp.json` 已由 `.gitignore` 忽略，但仍可能在屏幕截图、远程协作时被看到。约定：

- **所有 `Authorization` / `_TOKEN` / `_PASSWORD` / `_KEY` / `Basic …` 字段必须用 `${env:XXX}` 引用**，禁止明文
- **结构示范 + 占位符**：参考 `.cursor/mcp.json.example` 与 `.claude/mcp.json.example`
- **真实凭据统一存到 `~/.ai-config/mcp-secrets.env`**（`chmod 600`，仅当前用户可读）
  - 命名建议：`GITEA_MCP_TOKEN`、`JENKINS_BASIC_AUTH`（base64 of `user:apitoken`）、`MYSQL_PASSWORD[*]`、`OBSIDIAN_API_KEY`、`MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY`、`APIFOX_ACCESS_TOKEN`
  - 使用方式：在 `~/.zshrc` 末尾追加 `source ~/.config/zh-cloud/mcp-secrets.env`，或每次手动 source
- **`~/.claude/settings.json` 里 `mcpServers` 段同样改为 `${env:XXX}`**（该文件不入 git，但仍是本地泄漏面）
- **轮换**：在对应平台后台重生成后，更新 `mcp-secrets.env` 即可，配置文件无需改动

与 [`00-zh-cloud-core.mdc`](00-zh-cloud-core.mdc) 中「绝不泄露与提交敏感信息」互补。
