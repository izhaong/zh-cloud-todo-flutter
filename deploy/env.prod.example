# 宿主机：/izhaong/zh-cloud-prod/client/todo/env
# compose: docker compose -p client-todo -f docker-compose.yml --env-file env up -d

CLIENT_DOCKER_NETWORK=db-bridge-prod
NGINX_IMAGE=nginx:1.27-alpine
NGINX_UPSTREAM_YUDAO=http://yudao-server:48080
# npm client-todo.zh04.com → 本端口
CLIENT_PORT=58090
